name: Build and Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (例如: 0.0.1)'
        required: true
        default: '0.0.1'
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          # Desktop builds (with TUI)
          - goos: windows
            goarch: amd64
            artifact-name: syncgo_windows_amd64.exe
            build_tags: tui
          - goos: windows
            goarch: arm64
            artifact-name: syncgo_windows_arm64.exe
            build_tags: tui
          - goos: darwin
            goarch: amd64
            artifact-name: syncgo_darwin_amd64
            build_tags: tui
          - goos: darwin
            goarch: arm64
            artifact-name: syncgo_darwin_arm64
            build_tags: tui
          # Linux agent builds (lite, no TUI — smaller binary)
          - goos: linux
            goarch: amd64
            artifact-name: syncgo_linux_amd64
            build_tags: ""
          - goos: linux
            goarch: arm64
            artifact-name: syncgo_linux_arm64
            build_tags: ""

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "ver=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "ver=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT
          fi

      - name: Build for ${{ matrix.goos }}-${{ matrix.goarch }}
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          TAGS=""
          if [ -n "${{ matrix.build_tags }}" ]; then
            TAGS="-tags ${{ matrix.build_tags }}"
          fi
          go build $TAGS -ldflags="-s -w -X main.versionStr=${{ steps.version.outputs.ver }}" \
            -o ${{ matrix.artifact-name }} ./cmd/syncgo
          echo "Before UPX: $(du -h ${{ matrix.artifact-name }} | cut -f1)"

      - name: Install UPX
        if: matrix.goos != 'darwin'
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq upx-ucl
          upx --version | head -1

      - name: UPX compress (maximum)
        if: matrix.goos != 'darwin'
        run: |
          upx --best --lzma --no-progress ${{ matrix.artifact-name }}
          echo "After UPX: $(du -h ${{ matrix.artifact-name }} | cut -f1)"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact-name }}
          path: ${{ matrix.artifact-name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: List downloaded files
        run: |
          echo "Artifacts:"
          find ./artifacts -type f | sort

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "ver=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "ver=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT
          fi

      - name: Get current date
        id: date
        run: |
          echo "builddate=$(date +'%Y.%m.%d')" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.ver }}
          name: SyncGo v${{ steps.version.outputs.ver }}
          body: |
            ## SyncGo v${{ steps.version.outputs.ver }}

            跨平台增量文件同步工具 — rsync delta 算法，仅传输文件变化部分。

            ### 构建信息
            - 版本号: ${{ steps.version.outputs.ver }}
            - 构建日期: ${{ steps.date.outputs.builddate }}
            - CGO: 禁用（完全静态二进制，无 .so 依赖）
            - UPX: 最大压缩（--best --lzma，macOS 除外）

            ### 下载说明

            | 文件 | 平台 | 用途 |
            |------|------|------|
            | `syncgo_windows_amd64.exe` | Windows x64 | 主程序（含 TUI） |
            | `syncgo_windows_arm64.exe` | Windows ARM64 | 主程序（含 TUI） |
            | `syncgo_linux_amd64` | Linux x64 | 远端 Agent（精简版） |
            | `syncgo_linux_arm64` | Linux ARM64 | 远端 Agent（精简版） |
            | `syncgo_darwin_amd64` | macOS Intel | 主程序（含 TUI） |
            | `syncgo_darwin_arm64` | macOS Apple Silicon | 主程序（含 TUI） |

            ### Agent 部署

            Linux 二进制可直接作为远端 delta agent 使用：
            ```bash
            # 方式1: 自动部署（推荐）
            syncgo deploy-agent <server>

            # 方式2: 手动放置
            # 将 syncgo_linux_<arch> 放在 syncgo.exe 同级目录
            # deploy-agent 会自动识别并上传
            ```

            ### 校验和算法
            支持 xxh64（默认）/ xxh3 / md5 / sha256，amd64 平台含 SIMD 加速。
          files: |
            ./artifacts/syncgo_windows_amd64.exe/syncgo_windows_amd64.exe
            ./artifacts/syncgo_windows_arm64.exe/syncgo_windows_arm64.exe
            ./artifacts/syncgo_linux_amd64/syncgo_linux_amd64
            ./artifacts/syncgo_linux_arm64/syncgo_linux_arm64
            ./artifacts/syncgo_darwin_amd64/syncgo_darwin_amd64
            ./artifacts/syncgo_darwin_arm64/syncgo_darwin_arm64
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
